.comments_box.mb-3
  %h3 Comments
  = turbo_frame_tag "comments" do
    = render @scrolled_objects || commentable.comments
  -# Infinite scroll comments with turbo.
  = render "infinite_scroll/load_more"