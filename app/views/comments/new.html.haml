= turbo_frame_tag dom_id(@commentable, :new_comment) do
  .modal
    .modal_content
      .row.center-content
        .col-12
          = link_to @commentable, class: "close-btn", title: 'Cancel' do
            %i.fa-solid.fa-xmark
        .col-12
          = render 'form', commentable: @commentable, comment: @comment
          = render 'comments/box', commentable: @commentable
