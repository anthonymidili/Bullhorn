= turbo_frame_tag dom_id(likeable, "likes_form"),
class: dom_id(likeable, "likes_form") do
  - if current_user.has_liked?(likeable)
    = render "likes/unlike_button", likeable: likeable
  - else
    = render "likes/like_button", likeable: likeable
