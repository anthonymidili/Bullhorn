- provide :title, 'Comments'
.row.center-content
  .col-12.col-md-8
    = render @post
    .links.mb-3
      = link_to root_path(anchor: "post_#{@post.id}"), title: 'Back' do
        %i.fa-solid.fa-hand-point-left
        Back
      - if @post.images.attached?
        = link_to photos_user_path(@post.user), title: "#{@post.user.username} Photos" do
          %i.fa-solid.fa-images
          #{@post.user.username} Photos
    .mb-3
      #new_comment= render 'comments/form', commentable: @post, comment: @comment
    = render 'comments/box', commentable: @post, comment: (@comment ||= nil)
