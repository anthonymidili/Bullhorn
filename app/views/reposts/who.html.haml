= turbo_frame_tag dom_id(@reposted, "who_reposted") do
  .modal
    .modal_content
      .row
        .col-12
          = link_to @reposted, class: "close-btn", title: "Close" do
            %i.fa-solid.fa-xmark
          %h1 Who reposted this post
          %p
            = @reposted.users_who_reposted.count
            users
          = render 'users/badge', users: @reposted.users_who_reposted
